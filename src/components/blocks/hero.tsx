import { PlaceholderBlock } from "./placeholder";

export function HeroBlock() {
  return (
    <PlaceholderBlock
      blockId="hero"
      label="Hero — headline + sub + CTA primário"
      heightClass="min-h-[60vh]"
    />
  );
}
